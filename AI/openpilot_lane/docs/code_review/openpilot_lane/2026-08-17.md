# 2026-08-17 (KST) — openpilot_lane PoC 코딩 계획 표 (리뷰 아님)

> coding SOP §2/§6: 계획 단계 작성 → 구현 후 실제 줄 번호로 갱신 완료(v2; _REF 경로 수정으로 줄번호 +1 재동기화).
> 목적: openpilot supercombo ONNX 단독 실행으로 DSS 차선 인식 가능성 검증 (ADR: docs/adr/2026-08-17-openpilot-lane-poc.md)
> 위치: `src/AI/openpilot_lane/` (사용자 지시로 experiments/ 에서 이동, 2026-08-17 14:4x) — 루트 정본: docs/code_review/openpilot_lane/, 모듈 병기: src/AI/openpilot_lane/docs/code_review/openpilot_lane/. 캡처 데이터는 `experiments/openpilot_lane/data/` 유지.
>
> **v2 변경**: 합성 도로 검증으로 모델 출력 좌표계 실측 확정(x전방·y우+·z하+·원점=카메라) → `project_points` 부호반전·높이병진 제거, `z_up`/`DEFAULT_CAM_HEIGHT` 삭제, `EDGE_COLOR` 추가.
> **v6 변경**: 뷰어 종료 크래시(`_stop` 섀도잉→`_stop_evt`)·라이브 시간 문맥(0.2 s 페어링) 수정 — code_updates 참조.
> **v5 변경**: `parse_plan`·경로 밴드 렌더 신설(원본 카메라 뷰 유지 — 소프트웨어 확대는 정공법 위반으로 제거, mistake 2026-08-17-006). 뷰어 LaneModel 은 수신 해상도 자동 대응(cam_wh).
> **v3 추가**: `lane_viewer_qt.py` — PyQt5 실시간 차선 인식 뷰어(사용자 요청 17:15). 라이브(ROS 구독) + 재생(--frames-dir) 2모드, FOV·pitch 라이브 튜닝.

## 함수표 — lane_viewer_qt.py (v3 — 구현 완료, 실측 줄번호·GUI 검증 통과)

| # | 함수 | 입력 | 출력 | 기능 | 위치 |
| --- | --- | --- | --- | --- | --- |
| 15 | `LaneModel.__init__` | fov, pitch | - | ONNX 세션·워프 행렬·feat/desire 큐 초기화 (run_supercombo 함수 재사용) | lane_viewer_qt.py:36-45 |
| 16 | `LaneModel.set_calib` / `_apply_calib` | fov, pitch | - | K·워프 행렬 재계산 (라이브 튜닝, 다음 infer 시 적용·페어 리셋) | lane_viewer_qt.py:47-58 |
| 17 | `LaneModel.infer` | rgb(H,W,3) | overlay BGR, prob(4,), dt_ms | **v6**: 프레임 이력에서 0.2 s 전 프레임 선택 페어링(학습 문맥 정합) + 해상도 자동 대응 + plan 경로 밴드. 스레드 안전: infer 루프 전용 | lane_viewer_qt.py:64-102 |
| 18 | `RosGrabber` (Thread) | topic | latest() → (seq, rgb) | rclpy 백그라운드 스핀, 최신 프레임 보관 (reliable KeepLast10) | lane_viewer_qt.py:86-122 |
| 19 | `PlaybackGrabber` (Thread) | frames_dir, hz | latest() → (seq, rgb) | PNG 시퀀스 ~6 Hz 루프 재생 (DSS 없이 데모) | lane_viewer_qt.py:125-154 |
| 20 | `InferLoop` (Thread) | grabber, model | result_q 에 (overlay, prob, dt) | 최신 프레임만 소비(백로그 드롭), GUI 비블록 | lane_viewer_qt.py:157-186 |
| 21 | `LaneViewerWindow.__init__` | model, infer_loop, mode, fov, pitch | - | 영상 QLabel + 차선확률 4바 + FOV/pitch 스핀박스 + 상태바, 50 ms QTimer 폴링 | lane_viewer_qt.py:191-253 |
| 22 | `LaneViewerWindow._on_calib` / `._tick` | - | - | 캘리브레이션 반영 / result_q 폴링 → QImage 갱신·확률바·FPS 표시 | lane_viewer_qt.py:255-279 |
| 23 | `lane_viewer_qt.main` | `--fov --pitch --frames-dir --topic` | exit code | 모드 선택(라이브/재생), Qt 앱 기동, 종료 시 rclpy 정리 | lane_viewer_qt.py:282-321 |

## 함수표

| # | 함수 | 입력 | 출력 | 기능 | 위치 |
| --- | --- | --- | --- | --- | --- |
| 1 | `CaptureNode.__init__` | `out_dir` | - | `/dss/sensor/camera/rgb` 구독(reliable KeepLast10, 브리지 QoS 일치), 저장 폴더 준비 | capture_frames.py:23-35 |
| 2 | `CaptureNode.on_image` | `sensor_msgs/Image` | PNG+CSV 부수효과 | rgb8 프레임을 `frames/%06d.png` 저장, `stamps.csv` 에 (idx, stamp) append | capture_frames.py:37-47 |
| 3 | `CaptureNode.close` | - | - | CSV 파일 닫기 | capture_frames.py:49-50 |
| 4 | `capture_frames.main` | `--out`, `--duration` | exit code(프레임 0=1) | rclpy 초기화, duration 경과 시 종료 | capture_frames.py:53-75 |
| 5 | `rot_from_euler` | euler (3,) | R (3,3) | openpilot orientation 등가 재구현 (Rz·Ry·Rx) | run_supercombo.py:56-63 |
| 6 | `get_warp_matrix` | euler, cam_K, `bigmodel_frame` | M (3,3) | openpilot model.py:65-70 등가 — 모델 입력 픽셀→카메라 픽셀 사상 | run_supercombo.py:65-70 |
| 7 | `warp_to_model` | rgb(480,640,3), M | (256,512,3) | cv2.warpPerspective(WARP_INVERSE_MAP) 로 모델 입력 프레임 생성 (RGB 도메인, 기하 동일) | run_supercombo.py:73-76 |
| 8 | `rgb_to_yuv6ch` | rgb(256,512,3) | (6,128,256) uint8 | BT.601 YUV420(camerad rgb_to_nv12 등가) + frames_to_tensor 등가 6채널 패킹 | run_supercombo.py:79-91 |
| 9 | `sigmoid` | x | σ(x) | 확률 변환 | run_supercombo.py:94-95 |
| 10 | `parse_mdn_lanes` | 출력벡터(2576,) | lanes(4,33,2), prob(4,), edges(2,33,2) | output_slices 로 mean 절반 취득, prob=sigmoid 후 [1::2] (fill_model_msg.py:107 등가) | run_supercombo.py:98-105 |
| 10a | `parse_plan` | 출력벡터(2576,) | plan 위치 (33,3) | **v5 신설** — plan mean 앞 3열(x,y,z) = 주행 경로 | run_supercombo.py:108-111 |
| 11 | `project_points` | pts(N,3), cam_K, euler, cam_height=0 | 픽셀 (N,2), valid | calib 3D→픽셀. **실측 확정 좌표계(x전방·y우+·z하+·원점=카메라)** — 부호반전·높이병진 없음 | run_supercombo.py:114-125 |
| 12 | `draw_overlay` | bgr, lanes, prob, edges, cam_K, euler, cam_height, prob_thr, **plan, path_width** | BGR 오버레이 | 차선 4색 폴리라인 + prob 라벨 + 가장자리(빨강) + **v5: comma UI 스타일 주행경로 반투명 밴드(plan ±0.9 m)** | run_supercombo.py:128-164 |
| 13 | `run_sequence` | frames 경로, cam_K, euler, opts | lanes.json + overlays/*.png | 큐 유지(feat 24 시프트·desire 25 zeros), 연속 프레임 페어, ONNX 추론 루프 | run_supercombo.py:167-210 |
| 14 | `calibrate_sequence` | frames, cam_K, euler0, stride, speed_min | dict(pitch/yaw delta·total, n, trans) 또는 None | **v4 신설** — calibrationd 원리, 실측 수렴 검증(잔차 0.127→0.018, 최종 pitch 0.49) | run_supercombo.py:213-250 |
| 15 | `run_supercombo.main` | `--frames-dir --out --fov --cam-height --pitch --stride --calibrate` | exit code | argparse, FOV→fl intrinsics. `--calibrate` 분기(calibration.json 저장) | run_supercombo.py:253-293 |

## 전역변수표

| # | 변수 (상수) | 사용처 | 용도 | 위치 |
| --- | --- | --- | --- | --- |
| 1 | `MODEL_PATH` (references/commaai/openpilot/driving_supercombo.onnx) | #13 | ONNX 모델 경로 (SHA256=lfs oid 검증됨) | run_supercombo.py:33 |
| 2 | `SLICES` (output_slices.json 로드) | #10,#13 | 출력 벡터 슬라이스 (1차 소스: ONNX 메타데이터 내장 pickle) | run_supercombo.py:35-36 |
| 3 | `X_IDXS` (33,) = 192·(i/32)² | #12 | 차선 지점 종방향 거리 (constants.py 등가) | run_supercombo.py:39 |
| 4 | `MODEL_W/H`, `MEDMODEL_CY`, `MEDMODEL_K`, `SBIGMODEL_K` | #6 | 모델 입력 intrinsics (model.py:10-40 전사) | run_supercombo.py:41-44 |
| 5 | `VIEW_FROM_DEVICE` (3,3) | #6,#11 | device(x전방,y우,z하)→view(x우,y하,z전방) 축 치환 (camera.py 전사) | run_supercombo.py:47 |
| 6 | `LANE_COLORS` (4,) / `EDGE_COLOR` | #12 | 오버레이 색 (좌외·좌내·우내·우외 / 도로 가장자리) | run_supercombo.py:50-51 |
| 7 | `DEFAULT_FOV_DEG` = 90.0 ⚠추정 | #14 | DSS 카메라 HFOV 초기 추정 — CameraInfo 부재, 시각 튜닝 대상 | run_supercombo.py:53 |

## 의존성

| 패키지 | 상태 | 용도 |
| --- | --- | --- |
| onnxruntime 1.23.2 | 기설치 | supercombo 추론 (CPU) |
| opencv-python 4.9.0 | 기설치 | 워프·YUV 변환·오버레이 |
| rclpy (Humble) | 기설치 | capture_frames.py 구독 전용 |

신규 설치 0건. 저장소 기존 패키지 무변경.

## 검증 스냅샷 (2026-08-17)

- 합성 직선도로(자체 투영수학으로 렌더, 30프레임): 내측 차선 확률 **0.42/0.39**, 오버레이가 노면 차선 위 정렬 ✓ — 파이프라인(워프→YUV 패킹→ONNX→파싱→투영) 정상 판정.
- DSS 정지 장면(121프레임, 차로 밖 정차·pitch 미보정): 확률 0.03~0.06 — 장면 조건 기인(파이프라인 아님).
- **DSS 주행 데이터(298프레임, 전진 40 s, `data/20260817_drive1/`)**: pitch 0.35 rad 에서 내측 차선 확률 평균 **0.52/0.34, 피크 0.78/0.51** — 오버레이가 실제 노면 표시(황색 중앙선 우측·백색 점선)를 따라 정렬 ✓. **DSS 에서 openpilot 차선 인식 동작 실증.** 잔여: FOV 정밀 캘리브레이션(현재 90° ⚠추정), 차로 중앙 주행 데이터(현재 점선 걸침 주행), v3 갱신 예정 아님(수치는 code_updates 참조).
