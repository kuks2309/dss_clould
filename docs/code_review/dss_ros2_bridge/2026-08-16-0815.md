## 2026-08-16 08:15 (KST) — dss_ros2_bridge delta 리뷰 (리뷰 지적 반영 후)

**delta 리뷰다.** Core 5 항목 전체는 [2026-08-15.md](2026-08-15.md), 실기 구동 관측은 [2026-08-16.md](2026-08-16.md) 를 cross-reference 하고, 여기서는 **이번 수정이 바꾼 인벤토리와 findings 상태**만 다룬다.

### 코드 버전

- 비-git → 파일 내용 해시(SHA256 앞 16자리). 변경된 15개 파일만 표기.
- 변경 내역: [docs/code_updates/2026-08-16-review-fixes.md](../../code_updates/2026-08-16-review-fixes.md) · 결정: [ADR](../../adr/2026-08-16-review-fixes.md)

| 파일 | 08-16 08:15 | 직전 |
| --- | --- | --- |
| src/DSSToROSImage.cpp | `1a1acff809bbe61d` | `16ab80eb77902dc1` |
| src/DSSToROSStereoCameraColor.cpp | `8ccf51d21c486a8a` | `8de4d750e06dbdeb` |
| src/DSSToROSStereoCameraDepth.cpp | `9052d0b8edbbe945` | `9b1436edaf4aba52` |
| src/DSSToROSStereoCameraInfra1.cpp | `49164bc42836cdbd` | `c61e5cd93e8a7107` |
| src/DSSToROSStereoCameraInfra2.cpp | `a489c0e6fae4a660` | `88e932c9567b57b4` |
| src/DSSToROSIMU.cpp | `6baf37048eb759ae` | `912eb1c34747b559` |
| src/DSSToROSPointCloud.cpp | `b35ba8652270ce8a` | `4bf5107245cb7bad` |
| src/DSSToROSGps.cpp | `4c6746044913abc3` | `4ef20d8f2187eef7` |
| src/DSSToROSClock.cpp | `f237dbe3b4fee346` | `754a4e48bfbc6a85` |
| src/DSSDemo.cpp | `79e70df0ccdb80a4` | `737f7bdd7863c883` |
| src/DSS.VSSClient.cpp | `e59dd8b201e37b14` | `a6e7796ecb16cf26` (08-15) |
| src/DSS.VSSClient.h | `ee52f7875823c153` | `7ce386ba4e65ef0b` (08-15) |
| CMakeLists.txt | `258bc4076d0284e6` | `7c2123d2233f594c` (08-15) |
| package.xml | `4d0e7ee490c07576` | `a7922201a5cf5e3a` (08-15) |

### 분석 분기 명시

- 분기: 전체 구조 분석의 **delta** — 변경 파일 15개
- 감지된 도메인: ros2-review / concurrency

---

## 인벤토리 변동

### 3. 함수 리스트 (delta) — 추가·삭제·이동만

| # | 함수 | 입력 | 출력 | 기능 | 위치 |
|---|------|------|------|------|------|
| 10 | ~~`DSSToROSPointCloudNode::createPointCloud`~~ | — | — | **삭제됨** (호출부 0. 함수 #12 의 이전 세대) | (없음) |
| 36 | `DSSToROSIMUNode::normalizeOrientation` | `sensor_msgs::msg::Imu&` (in/out) | void | 노름 0 이면 단위 쿼터니언 대체, `1e-3` 초과 이탈이면 경고(throttle) 후 정규화 | src/DSSToROSIMU.cpp:60 |
| 37 | `buildVssJson` (익명 네임스페이스) | `char(&)[256]`, `format`, `a`, `b=nullptr` | bool | VSS 요청 JSON 을 고정 버퍼에 조립하고 **잘림 여부를 반환** | src/DSS.VSSClient.cpp:178 |
| 31 | `DSSVssClient::unsubscribe` (static) | `natsSubscription*&` | void | 전역 클로저 맵에서 항목 제거·해제 후 구독 파괴 (헤더 inline → `.cpp` 정의로 **이동**) | src/DSS.VSSClient.cpp:365 |
| 30 | `DSSVssClient::subscribeSensor` | `subject`, `OnRawMessage`, `natsStatus*` | `natsSubscription*` | 함수 #29 로 **위임**(본문 중복 제거) | src/DSS.VSSClient.cpp:359 |
| 17 | `DSSVssClient::DSSVssClient` (private 생성자) | 없음 | — | `= default` — **연결하지 않는다**(연결은 함수 #19 만) | src/DSS.VSSClient.cpp:21 |
| 15 | `main` (10개 타깃 공통) | `argc`, `argv` | int | `init` → **try** `spin` **catch** → FATAL 로그 + `shutdown` + **return 1** | 각 파일 말미 |

함수 #1~#9·#11~#14·#16·#18~#29·#32~#35 는 시그니처 불변 — [2026-08-15 표](2026-08-15.md#3-함수-리스트) cross-ref.

### 4. 전역 변수 / 모듈 상수 (delta)

| # | 변수 | 사용처(함수) | 기능 | 위치 |
|---|------|--------------|------|------|
| 10 | `kVssJsonCap` (상수, 256) | 함수 #23·#24·#25·#37 | VSS 요청 JSON 버퍼 크기 | src/DSS.VSSClient.cpp:176 |
| 11 | `kQuatNormTolerance` (상수, 1e-3) | 함수 #36 | 단위 쿼터니언 판정 허용 오차 | src/DSSToROSIMU.cpp:48 |
| 12 | `kBytesReadPerPoint` (상수, 14) | 함수 #12 | 포인트당 실제로 읽는 바이트 수(경계 가드 기준) | src/DSSToROSPointCloud.cpp:57 |
| 13 | `DSSToROSIMUNode::throttle_clock_` (가변 멤버) | 함수 #36 | 경고 throttle 용 `RCL_STEADY_TIME` 시계 — sim time 정지 시에도 경고가 나오게 | src/DSSToROSIMU.cpp:44 |

전역 #9 `DT_NS` 등 기존 항목 불변.

### 5. 의존성 3-tier (delta)

| Tier | 대상 | 버전/제약 | 부재 시 동작(필수/선택) | 근거(파일:line) |
| --- | --- | --- | --- | --- |
| 빌드 | `sensor_msgs` · `rosgraph_msgs` · `OpenCV` · `protobuf` · `nlohmann-json` | — | **package.xml 에 선언됨** → `rosdep install` 로 설치 가능 (이전: 미선언으로 설치 불가) | package.xml:15-22 |
| 빌드 | `Threads` | — | `find_package(Threads REQUIRED)` 로 명시 — 전이 정의 의존 제거 | CMakeLists.txt:20 |
| 빌드 | `cnats` (`libnats`) | `find_library` | **미발견 시 구성 단계에서 `FATAL_ERROR`** 로 즉시 실패(이전: 링크 단계 불명확 오류) | CMakeLists.txt:27-30 |
| 빌드 | `Protobuf` | `3.15`, `Protobuf_DIR` CACHE | `-DProtobuf_DIR=...` 로 덮어쓰기 가능(이전: 절대경로 하드코딩) | CMakeLists.txt:10 |
| 런타임 필수 | NATS 서버 | — | **프로세스 종료(코드 1)** + FATAL 로그 (이전: 무동작 생존) | 함수 #1·#15 |

---

## findings 상태 갱신

severity 분포: Critical 0 / High 1 / Medium 3 / Low 2 / Info 1
Verdict: REQUEST CHANGES

> 잔존 항목이 있어 `COMMENT` 가 아니다. 작성 lane 이므로 `APPROVE` 불가(SOP rule 11) — 반영분 검증은 별도 lane 재리뷰 필요.

### 해결

- **함수 #1·#7a — [race][논리][해결] 퍼블리셔 순서 race (High #1)** — `create_publisher` 가 `subscribeTopicRaw` 앞으로. 9개 노드 전부 확인(pub 줄번호 < sub 줄번호).
- **함수 #23·#24·#25 — [논리][해결] `sprintf` 오버플로 (High #2)** — `snprintf` + 잘림 검사. `sprintf` 잔존 0건(grep).
- **함수 #27 — [논리][해결] closure 캐스팅 UB (High #3)** — `OnRawMessage*` 로 정정.
- **함수 #19·#32 — [논리][해결] `start()` 무시 (High #4)** — 실측 확인: 데모 로그가 `connected: nats://100.80.80.15:4222` (이전 기본 인자 `172.25.96.1` 는 이 환경에서 도달 불가였음).
- **함수 #1 — [논리][runtime][해결] 좀비 노드 (High #5)** — 격리 시험에서 FATAL 2줄 후 **종료 코드 1**.
- **의존성 빌드 tier — [논리][해결] package.xml 누락 (High #6)** — 5개 선언 추가 + CMake 3건 정정.
- **함수 #31 — [논리][해결] `unsubscribe` 이중 해제 (High #7)** — 맵 정리 후 파괴.
- **함수 #9·#36 — [논리][해결] 비정규화 쿼터니언 (신규 High)** — 실측 200 샘플 노름 전부 `1.000000`.
- **함수 #12 — [논리][해결] 경계 검증 부재 (Critical→Medium 재평가분)** — `point_step >= 14` · `data.size()/step >= width` 가드. 나눗셈 비교로 `width * step` 오버플로 회피.
- **함수 #32b — [topology][해결] 고아 구독 (Medium #1)** — 구독 토픽을 `/dss/sensor/lidar3d` 로.
- **함수 #1 — [논리][해결] 비상수 포맷 문자열 (Medium #3)** — 11곳 `"%s"` 적용.
- **함수 #29·#30 — [품질][해결] 중복 함수 (Medium #5)** — 위임으로 본문 단일화.
- **함수 #10·#12 — [품질][해결] dead code (Medium #6)** — 121줄 삭제 + 미사용 지역변수 제거.
- **함수 #33 관련 — [스타일][해결] 데모의 하드코딩 주소 (Medium #7 일부)** — `getDefaultGateway()` 사용으로 브리지와 출처 통일. **함수 #33 자체의 하드코딩은 `[잔존]`**.

### 잔존

**함수 #1~#7·#7a — [품질][SOLID][잔존] 브리지 골격 9벌 중복 (High)**
   미반영. 이번 수정이 그 대가를 다시 보여줬다 — High #1·#5, Medium #3 을 각각 9개 파일에 같은 방식으로 적용해야 했다(변환 스크립트로 처리). 기반 클래스 추출은 회귀 위험이 커 별도 작업 단위로 둔다.

**함수 #1 — [topology][잔존] 스테레오 4개 노드의 원천 subject 부재 (Medium, 이전 High 에서 조정)**
   NATS 에 `dss.sensor.camera.stereo.*` 가 없어 4개 노드가 무동작. 다만 이제 연결 실패는 프로세스 종료로 드러나므로 "연결은 됐는데 데이터가 없다" 만 남는다 — 미수신 감시가 필요하다.

**함수 #36 — [논리][잔존] 쿼터니언 원천 이상 (High → 브리지 측 완화됨)**
   브리지는 정규화하고 경고하지만, 노름 0.707 의 원인(성분 누락 의심)은 DSS 측 확인이 남았다. 정규화된 값도 원천이 틀리면 틀린 자세다.

**[topology][잔존] `dss.odom` · `dss.egoVehicle.snapShot` 미브리지 (Medium)** — 변동 없음.

**함수 #13 — [논리][잔존] GPS 경도 부호 의심 · covariance 전 0 (Low)** — 변동 없음.

**함수 #33 — [논리][잔존] `getDefaultGateway` 이름-동작 불일치·IP 하드코딩 (Low)** — 파라미터화 미반영.

### 신규 (이번 수정이 만든 것)

**[runtime][신규] `launch.py` 에 `respawn` 정책이 없다 (Info)**
   연결 실패가 이제 프로세스 종료이므로, NATS 가 노드보다 늦게 뜨는 환경에서는 노드가 죽은 채로 남는다. 기동 순서 의존은 절차서에 있으나(A-8 1단계 → 4단계), 선언적 재기동(`respawn=True`)을 두면 순서 의존이 사라진다.

---

### 검증 기록

| 항목 | 결과 |
| --- | --- |
| 빌드 | 성공 (13.0 s) |
| 정상 구동 | 노드 5개 생존 · lidar3d 7.1 Hz · camera/rgb 5.3 Hz · imu 141 Hz · /clock 141 Hz |
| 쿼터니언 | 200 샘플 노름 min=max=avg=`1.000000` |
| 데모 접속 | `nats://100.80.80.15:4222` 연결 확인 |
| 실패 경로 | netns 격리 → FATAL 2줄 → 종료 코드 **1** |
| 미수행 | 스테레오 4개 노드 기능 검증(원천 부재) · 단위 테스트(여전히 0건) |

---
